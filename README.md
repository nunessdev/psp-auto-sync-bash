# psp-auto-sync-bash
Automatically syncs game save files with desired folder once the PSP is connected and its system mounted to the PC (Linux).

TODO:
Add automatic backup before sync (to be safe from unwanted overwrites).
Error handling (more logging).
